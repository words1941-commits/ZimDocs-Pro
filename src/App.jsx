import React from 'react'
export default function App(){
  return (
    <div style={{fontFamily:'Inter, system-ui, sans-serif',padding:24}}>
      <h1>ZimDocs-Pro (source)</h1>
      <p>This repo contains a source skeleton (Vite + React) and a prebuilt /docs static site for GitHub Pages.</p>
    </div>
  )
}